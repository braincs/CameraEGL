<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <Button
        android:id="@+id/btnRecroding"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Record"
        android:onClick="recording"/>

    <Button
        android:id="@+id/btnMirror"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Mirror"
        android:onClick="mirror"
        android:layout_alignTop="@+id/btnRecroding"
        android:layout_toRightOf="@+id/btnRecroding"/>

    <Button
        android:id="@+id/btnHide"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Hide"
        android:onClick="hide"
        android:layout_alignTop="@+id/btnRecroding"
        android:layout_toRightOf="@+id/btnMirror"/>

    <com.attrsc.braincs.cameraview.CameraEGLView
        android:id="@+id/cameraEglView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:camera_id="1"/>
</RelativeLayout>
